<h2>Your Shopping Cart</h2>

<%=@products.inspect %>

<% if @products.nil? %>
  <strong>Cart is Currently Empty</strong>
<% else %>
  <ul>
    <% @products.each do |product| %>
      <li>
        <a href=<%= "/products/#{ product.id }" %>>
        <%= product.name %>
        </a>:   <em><%= product.price %></em>
      </li>
    <% end %>
  </ul>
<% end %>
